[project]
name = "equator"
version = "0.1.0"
description = "Shared prompt_toolkit TUI foundation for agent and beetle"
requires-python = ">=3.13"

dependencies = [
    "prompt-toolkit>=3.0.51",
]

[tool.uv]
package = true

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
